<UserControl x:Class="ClientApp.Controls.PasswordBoxWithToggle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ClientApp.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="Root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Hidden Password TextBox -->
        <TextBox
            IsReadOnly="True"
            x:Name="VisiblePasswordBox"
            Text="{Binding PasswordText, ElementName=Root, Mode=TwoWay}"
            Visibility="{Binding IsPasswordVisible, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}"
            VerticalAlignment="Center" />

        <!-- PasswordBox -->
        <PasswordBox
            x:Name="PasswordBox"
            Visibility="{Binding IsPasswordVisible, ElementName=Root, Converter={StaticResource InvertedBooleanToVisibilityConverter}}"
            VerticalAlignment="Center"
            PasswordChanged="PasswordBox_PasswordChanged" />

        <!-- Toggle Button -->
        <ToggleButton
            Grid.Column="1"
            IsChecked="{Binding IsPasswordVisible, ElementName=Root}"
            Style="{StaticResource MaterialDesignFlatPrimaryToggleButton}"
            VerticalAlignment="Center"
            Margin="8,0,0,0">
            <materialDesign:PackIcon
                Kind="{Binding IsPasswordVisible, ElementName=Root, Converter={StaticResource EyeIconConverter}}"
                Width="15"
                Height="15" />
        </ToggleButton>
    </Grid>
</UserControl>
