<UserControl x:Class="ClientApp.Views.Controls.HighScoreUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientApp.Views.Controls"
             mc:Ignorable="d" 
             TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}" Padding="10" Margin="0,5,0,5" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>

                <!-- Rank -->
                <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Rank}" 
                           FontSize="15" 
                           FontWeight="Bold"/>
                                        <TextBlock Text=" #" 
                           FontSize="13"  />
                </StackPanel>

                <!-- Player Info -->
                <StackPanel Grid.Column="1" Margin="10,0">
                    <TextBlock Text="{Binding PlayerUsername, StringFormat='User: {0}'}" 
                           FontSize="15" 
                           FontWeight="Bold" />
                    <TextBlock Text="{Binding GameName, StringFormat='Game: {0}'}" 
                           FontSize="13"  />
                </StackPanel>

                <!-- Total Score -->
                <TextBlock Text="{Binding TotalScore}" 
                       Grid.Column="2" 
                       FontSize="15" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center" />
            </Grid>
        </Border>

    </Grid>
</UserControl>
