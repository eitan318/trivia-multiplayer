<UserControl x:Class="ClientApp.Views.Controls.PersonalStatisticsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">

    <StackPanel Margin="0,10,0,0">
        <UniformGrid Columns="2" Margin="0,5,0,5" VerticalAlignment="Top" HorizontalAlignment="Stretch">
            <TextBlock Text="Username:" FontWeight="Bold" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" />
            <TextBlock Text="{Binding UserName}" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" HorizontalAlignment="Center" Margin="10,0,0,0" />

            <TextBlock Text="Games Played:" FontWeight="Bold" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" />
            <TextBlock Text="{Binding GamesPlayed}" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" HorizontalAlignment="Center" Margin="10,0,0,0" />

            <TextBlock Text="Total Answers:" FontWeight="Bold" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" />
            <TextBlock Text="{Binding TotalAnswers}" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" HorizontalAlignment="Center" Margin="10,0,0,0" />

            <TextBlock Text="Correct Answers:" FontWeight="Bold" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" />
            <TextBlock Text="{Binding TotalCorrectAnswers}" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" HorizontalAlignment="Center" Margin="10,0,0,0" />

            <TextBlock Text="Avg. Answer Time:" FontWeight="Bold" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" />
            <TextBlock Text="{Binding AvgAnswerTime}" Foreground="{DynamicResource MaterialDesignBody}" FontSize="16" HorizontalAlignment="Center" Margin="10,0,0,0" />
        </UniformGrid>
    </StackPanel>
</UserControl>
                <!-- Personal Statistics -->
