<UserControl x:Class="ClientApp.Views.Controls.RoomPreviewUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
             d:DesignHeight="200" d:DesignWidth="300">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="{Binding RoomData.RoomName}" FontWeight="Bold" FontSize="16" />
        <TextBlock Text="{Binding PlayersInfo}" />
        <TextBlock Text="{Binding RoomData.NumOfQuestionsInGame, StringFormat='Questions: {0}'}" />
        <TextBlock Text="{Binding RoomData.TimePerQuestion, StringFormat='Time per Question: {0} sec'}" />
        <TextBlock Text="{Binding StatusInfo, StringFormat='Status: {0}'}" /> 
    </StackPanel>
</UserControl>
