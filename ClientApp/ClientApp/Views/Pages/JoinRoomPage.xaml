<local:NavigablePage x:Class="ClientApp.Views.Pages.JoinRoomPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClientApp.Views.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
      Title="JoinRoomPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2.5*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- ComboBox with custom DataTemplate -->
        <ComboBox 
            Grid.Column="1" 
            ItemsSource="{Binding Rooms}" 
            SelectedItem="{Binding SelectedRoom}" >
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding MaxPlayers, StringFormat='Max Players: {0}'}"/>
                        <TextBlock Text="{Binding NumOfQuestionsInGame, StringFormat='Questions: {0}'}"/>
                        <TextBlock Text="{Binding TimePerQuestion, StringFormat='Time per Question: {0} sec'}"/>
                        <TextBlock Text="status?" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>

        </ComboBox>


        <StackPanel Grid.Row="1" Grid.Column="1">
            <Button Content="Refresh" Margin="8" Command="{Binding RefreshCommand}"/>
            <Button Content="Join" Margin="8" Command="{Binding JoinCommand}"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" />
        </StackPanel>
    </Grid>
</local:NavigablePage>
