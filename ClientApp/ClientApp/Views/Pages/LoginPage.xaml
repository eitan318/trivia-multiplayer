<Page x:Class="ClientApp.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:controls="clr-namespace:ClientApp.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <Grid Background="{DynamicResource MaterialDesignPaper}">


        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Centered Card -->
        <materialDesign:Card Grid.Row="1" Width="400" Padding="20" >

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

                <TextBlock Text="Welcome! Please Log In" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           TextAlignment="Center" 
                           Margin="0,0,0,20"
                           Foreground="{DynamicResource MaterialDesignBody}" />

                <!-- Username TextBox -->
                <TextBox
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                     x:Name="UsernameTB"
                     materialDesign:HintAssist.Hint="Username" 
                     Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                     Margin="0,10" />

                <!-- PasswordBox -->
                <controls:PasswordBoxWithToggle
                    Margin="0,10"
                    PasswordText="{Binding Password, Mode=TwoWay}"
                    Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                    materialDesign:HintAssist.Hint="Password"/>

                <!-- Forgot password Button -->
                <TextBlock>
                    <Hyperlink Command="{Binding NavigateToForgotPasswordCommand}" >Forgot My Password
                    </Hyperlink>
                </TextBlock>

                <!-- Login Button -->
                <Button Content="Login" Margin="0,20" Padding="10,5" 
                    Command="{Binding LoginCommand}"/>

                <!-- Sign Up Button -->
                <Button Content="Sign Up" Padding="10,5" Margin="0,10" 
                    Command="{Binding NavigateToSignupCommand}" 
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="Red" 
                           TextAlignment="Center"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Page>
