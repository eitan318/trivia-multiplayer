<UserControl x:Class="ClientApp.Views.Pages.MemberRoomView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClientApp.Views.Pages"
      xmlns:controls="clr-namespace:ClientApp.Views.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Admin Display -->
            <RowDefinition Height="*"/>
            <!-- Players List -->
            <RowDefinition Height="Auto"/>
            <!-- Refresh Button -->
        </Grid.RowDefinitions>

        <!-- Admin Display -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Admin:" FontSize="16" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <controls:PlayerUserControl DataContext="{Binding Admin}"  Margin="0,0,100,0"/>
            <controls:RoomDataUserControl DataContext="{Binding RoomData}"/>
        </StackPanel>

        <!-- List of Players -->
        <GroupBox Header="Players" Grid.Row="1" Padding="10" Margin="0,0,0,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Players}">
                    <!-- Layout for player items -->
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="4" HorizontalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <!-- Template for each player item -->
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                             <controls:PlayerUserControl />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>


            </ScrollViewer>
        </GroupBox>


        <StackPanel Grid.Row="2" >
            <!-- Leave Button -->
            <Button Content="Leave Room" Command="{Binding LeaveRoomCmd}"
                HorizontalAlignment="Center" Padding="10,5" Margin="20"/>
            
            <!-- Refresh Button -->
            <Button Content="Refresh" Command="{Binding RefreshCmd}" 
                HorizontalAlignment="Center" Padding="10,5" />


        </StackPanel>
    </Grid>
</UserControl>
