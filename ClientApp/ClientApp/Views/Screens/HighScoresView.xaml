<UserControl x:Class="ClientApp.Views.Screens.HighScoresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientApp.Views.Screens"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:ClientApp.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Style="{StaticResource UserControlStyle}"
>
    <DockPanel>
        <!-- Top Bar -->
        <controls:TopBar DataContext="{Binding TopBarVM}" DockPanel.Dock="Top" />

        <!-- Main Content -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <!-- Title -->
                <RowDefinition Height="*" />
                <!-- Leaderboard -->
                <RowDefinition Height="Auto" />
                <!-- Footer -->
            </Grid.RowDefinitions>


            <Border Style="{StaticResource TitleBorderStyle}">
                <TextBlock Text="🏆 High Scores" Style="{StaticResource TitleStyle}" />
            </Border>




            <!-- Leaderboard -->
            <Border Grid.Row="1"
                Margin="0,20,0,20"
                Padding="10"
                CornerRadius="10"
                BorderThickness="0"
                BorderBrush="{DynamicResource MaterialDesign.Brush.Primary}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                    <ItemsControl ItemsSource="{Binding ResponseList}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="1"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <!-- High Score Item -->
                                <Border Style="{StaticResource ListItemStyle}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="2*" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Rank -->
                                        <TextBlock Grid.Column="0"
                                               Text="{Binding Rank, StringFormat='#{0}'}"
                                               FontSize="16"
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center" />

                                        <!-- Player Info -->
                                        <StackPanel Grid.Column="1" Margin="10,0">
                                            <TextBlock Text="{Binding PlayerUsername, StringFormat='User: {0}'}" 
                                                   FontSize="14" 
                                                   FontWeight="Bold" />
                                            <TextBlock Text="{Binding RoomName, StringFormat='Game Room: {0}'}" 
                                                   FontSize="13" />
                                            <TextBlock Text="{Binding StartTime, StringFormat='Start Time: {0:yyyy-MM-dd HH:mm}'}" 
                                                   FontSize="13" />
                                        </StackPanel>

                                        <!-- Total Score -->
                                        <TextBlock Text="{Binding TotalScore, StringFormat='Score: {0}'}"
                                               Grid.Column="2"
                                               FontSize="16"
                                               FontWeight="Bold"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Center" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- Error Message -->
            <TextBlock Text="{Binding ErrorMessage}"
                       Style="{StaticResource ErrorMessageStyle}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
        </Grid>
    </DockPanel>
    
</UserControl>
