<UserControl x:Class="ClientApp.Views.Screens.MenuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <!-- StackPanel to center menu buttons -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding Timer.FormattedTime, StringFormat='Timer: {0}'}" 
                       FontWeight="Bold" FontSize="16" Margin="10,0" />
            <!-- Create Room Button -->
            <Button Content="Create Room" 
                    Command="{Binding NavToCreateRoomCmd}" 
                    Margin="0,10"/>

            <!-- Join Room Button -->
            <Button Content="Join Room" 
                    Command="{Binding NavToJoinRoomCmd}" 
                    Margin="0,10"/>

            <!-- Statistics Button -->
            <Button Content="Statistics" 
                    Command="{Binding NavToStatisticsCmd}" 
                    Margin="0,10"/>

            <!-- Logout Button -->
            <Button Content="Logout" 
                    Command="{Binding LogoutCmd}" 
                    Margin="0,10"/>

            <!-- Error Message -->
            <TextBlock Text="{Binding ErrorMessage}" 
                   Foreground="{StaticResource ErrorBrush}" 
                   Visibility="{Binding ErrorMessage}" />
        </StackPanel>
    </Grid>
</UserControl>
