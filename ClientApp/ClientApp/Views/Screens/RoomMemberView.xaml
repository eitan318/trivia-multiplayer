<UserControl x:Class="ClientApp.Views.Screens.RoomMemberView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClientApp.Views.Screens"
      xmlns:controls="clr-namespace:ClientApp.Views.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Admin Display -->
            <RowDefinition Height="*"/>
            <!-- Players List -->
            <RowDefinition Height="Auto"/>
            <!-- Refresh Button -->
        </Grid.RowDefinitions>

        <!-- Admin Display -->
        <materialDesign:Card Grid.ColumnSpan="2" UniformCornerRadius="22" BorderBrush="{StaticResource MaterialDesignLightSeparatorBackground}" BorderThickness="3" Margin="0,0,0,10" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="Admin:" FontSize="16" FontWeight="Bold" Margin="10,0,10,0" />
                    <controls:PlayerUserControl DataContext="{Binding Admin}" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical">
                    <TextBlock Text="name"/>
                    <TextBlock Grid.Column="1" Text="{Binding RoomDataStore.CurrentRoomData.RoomName}"
                               FontWeight="Bold" FontSize="28" />
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Vertical">
                    <TextBlock Text="{Binding RoomDataStore.CurrentRoomData.MaxPlayers, StringFormat='Max Players: {0}'}" />
                    <TextBlock Text="{Binding RoomDataStore.CurrentRoomData.NumOfQuestionsInGame, StringFormat='Questions: {0}'}" />
                    <TextBlock Text="{Binding RoomDataStore.CurrentRoomData.TimePerQuestion, StringFormat='Time per Question: {0} sec'}" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
        

        <!-- List of Players -->
        <GroupBox Header="Players" Grid.Row="1" Padding="10" Margin="0,0,0,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Players}">
                    <!-- Layout for player items -->
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="4" HorizontalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <!-- Template for each player item -->
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <controls:PlayerUserControl />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>


        <StackPanel Grid.Row="2" >
            <!-- Leave Button -->
            <Button Content="Leave Room"
                    Command="{Binding LeaveRoomCmd}"
                    HorizontalAlignment="Center" Padding="10,5" Margin="20"
                    materialDesign:ButtonAssist.CornerRadius="16"
                    BorderThickness="3"
                    Width="200"
                    Height="40"
                    FontWeight="Bold"
                    Style="{StaticResource OperationButtonStyle}"/>
            
            
        </StackPanel>
    </Grid>
</UserControl>
