<UserControl x:Class="ClientApp.Views.Screens.StatisticsView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:ClientApp.Views.Screens"
                     mc:Ignorable="d"
                     d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Personal statistics" 
                    Command="{Binding NavToPersonalStatisticsCmd}"
                    Margin="0,10"
                    Style="{StaticResource MenuButtonStyle}"
                    />
            
            <Button Content="High scores"
                    Command="{Binding NavToHighScoresCmd}"
                    Style="{StaticResource MenuButtonStyle}"
                    Margin="0,10"
                    />
        </StackPanel>
    </Grid>
</UserControl>
