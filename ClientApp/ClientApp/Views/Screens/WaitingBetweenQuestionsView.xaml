<UserControl x:Class="ClientApp.Views.Screens.WaitingBetweenQuestionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ClientApp.Views.Screens"
             mc:Ignorable="d" 
             TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Enlarged Loading Animation -->
            <Border CornerRadius="100" Width="150" Height="150" Background="Transparent" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="5">
                <ProgressBar
                    IsIndeterminate="True"
                    Style="{StaticResource MaterialDesignCircularProgressBar}"
                    Width="120"
                    Height="120"
                    Value="0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center" />
            </Border>

            <!-- Enlarged Loading Text -->
            <TextBlock Text="Waiting for everyone to answer..."
                       FontSize="26"
                       FontWeight="Bold"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,10,0,0" />

            <!-- Enlarged Leave Room Button -->
            <Button Margin="0,30,0,0"
                    Content="Leave Room"
                    Style="{StaticResource OperationButtonStyle}"
                    Command="{Binding LeaveGameCmd}"
                    Width="200"
                    Height="60"
                    FontSize="20"
                    FontWeight="Bold"
                    HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
