<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- ============================
       FONT FAMILY DEFINITION
       Defines the main app font family to be used in styles
       ============================ -->
    <FontFamily x:Key="AppFont">Roboto Mono</FontFamily>

    <!-- ============================
       BUTTON STYLES
       ============================ -->

    <!--
    TextLikeButtonStyle:
    Makes a Button look like simple text (no background, no border),
    with a hand cursor and bold font.
  -->
    <Style x:Key="TextLikeButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- LoginToggleStyle inherits from TextLikeButtonStyle -->
    <Style x:Key="LoginToggleStyle" BasedOn="{StaticResource TextLikeButtonStyle}" TargetType="Button" />

    <!--
    OperationButtonStyle and LowProButtonStyle:
    Based on MaterialDesignOutlinedButton with rounded corners,
    bold font weight, and thicker borders for emphasis.
  -->
    <Style x:Key="OperationButtonStyle" BasedOn="{StaticResource MaterialDesignOutlinedButton}" TargetType="Button">
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="16"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="BorderThickness" Value="3"/>
    </Style>

    <Style x:Key="LowProButtonStyle" BasedOn="{StaticResource MaterialDesignOutlinedButton}" TargetType="Button">
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="16"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="BorderThickness" Value="3"/>
    </Style>

    <!--
    BackButtonStyle:
    Icon button with an arrow left icon, fixed size, and tooltip.
  -->
    <Style x:Key="BackButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
        <Setter Property="Width" Value="40" />
        <Setter Property="Height" Value="40" />
        <Setter Property="ToolTip" Value="Back" />
        <Setter Property="Content">
            <Setter.Value>
                <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Setter.Value>
        </Setter>
    </Style>

    <!--
    MenuButtonStyle:
    Larger outlined button with corner radius and scaling animation on mouse over.
  -->
    <Style x:Key="MenuButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="MinHeight" Value="120"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="16"/>
        <Setter Property="BorderThickness" Value="3"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.15"/>
                        <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.15"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.15"/>
                        <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.15"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <!-- ============================
       TEXTBLOCK STYLES
       ============================ -->

    <!--
    TitleStyle:
    Large bold centered text, used for main titles.
  -->
    <Style x:Key="TitleStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <!--
    SubtitleStyle:
    Slightly smaller semi-bold centered text with margin.
  -->
    <Style x:Key="SubtitleStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="20"/>
    </Style>

    <!--
    ErrorMessageStyle:
    Red centered text, smaller font size for error messages.
  -->
    <Style x:Key="ErrorMessageStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="Red"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- ============================
       HYPERLINK STYLE
       ============================ -->

    <Style x:Key="HyperlinkStyle" TargetType="Hyperlink">
        <Setter Property="Foreground" Value="LightBlue" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="Blue" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================
       USERCONTROL STYLE
       Applies font family and foreground for text elements in UserControl
       ============================ -->
    <Style TargetType="UserControl" x:Key="UserControlStyle">
        <Setter Property="TextElement.Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
        <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
    </Style>

</ResourceDictionary>
